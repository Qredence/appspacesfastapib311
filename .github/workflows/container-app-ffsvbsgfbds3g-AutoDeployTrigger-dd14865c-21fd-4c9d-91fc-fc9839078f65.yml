name: Trigger auto deployment for container-app-ffsvbsgfbds3g

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-ffsvbsgfbds3g-AutoDeployTrigger-dd14865c-21fd-4c9d-91fc-fc9839078f65.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPFFSVBSGFBDS3G_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPFFSVBSGFBDS3G_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPFFSVBSGFBDS3G_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPFFSVBSGFBDS3G_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPFFSVBSGFBDS3G_REGISTRY_PASSWORD }}
          containerAppName: container-app-ffsvbsgfbds3g
          resourceGroup: appspacesfastapib311ba15
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


